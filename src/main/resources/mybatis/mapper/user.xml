<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.koreait.blog.repository.UserRepository">

	<select id="selectUserInfo" resultType="User">
		SELECT UNO, NAME, PASSWORD, EMAIL, GRADE, REGDATE
		  FROM CH_USER
		 WHERE UNO = #{uno}
		 ORDER BY REGDATE DESC
	</select> 

	<select id="selectUserList" resultType="User">
		SELECT UNO, NAME, PASSWORD, EMAIL, GRADE, REGDATE
		  FROM CH_USER
	</select>
	
	<insert id="insertUser" parameterType="User">
		INSERT INTO CH_USER(UNO, NAME, PASSWORD, EMAIL, GRADE, REGDATE)
		VALUES ( #{uno}, #{name}, #{password} #{email}, #{grade} now() )  
	</insert>
	
	<update id="updateUser" parameterType="User">
		UPDATE CH_USER 
		   SET NAME = #{name}, PASSWORD = #{password}, EMAIL = #{email}, GRADE = #{grade}, REGDATE = now()
		 WHERE UNO = #{uno}  
	</update>
	
	<delete id="deleteUser">
		DELETE 
		  FROM CH_USER
		 WHERE UNO = #{uno} 
	</delete>
			
</mapper>